version: "3.8"

services:
  db:
    image: postgres:15
    container_name: tasks-db
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: tasks
    ports:
      - "5432:5432"
